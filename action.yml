name: 'Dad Joke Action'
description: 'Generates a dad joke'
outputs:
  joke: # Este nombre debe coincidir EXACTAMENTE con el de core.setOutput
    description: 'The generated dad joke'
runs:
  using: 'node24'
  main: 'dist/index.js'